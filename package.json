{
  "name": "james-oliver-wedding",
  "version": "1.0.0",
  "description": "James & Oliver's Wedding Celebration Site",
  "scripts": {
    "build": "node build-assets.js",
    "dev": "python3 -m http.server 8000",
    "deploy": "npm run build && echo 'Build complete. Deploy the dist/ folder to your hosting provider.'"
  },
  "devDependencies": {},
  "engines": {
    "node": ">=14.0.0"
  }
}
